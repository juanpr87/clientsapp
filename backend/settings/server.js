'use strict';

module.exports = {
  // Server hostname
  hostname: 'localhost',

  // Server port
  // If you'd like to test the services through the Swagger doc, 
  // you'll have to change the port in /swagger/swagger.json file
  hostport: 5000
}
